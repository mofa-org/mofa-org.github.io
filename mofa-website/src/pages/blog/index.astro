---
// 客户端根据语言自动重定向
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Redirecting to Blog...</title>
  <script>
    // 根据 localStorage 语言状态重定向
    (function() {
      const savedLang = localStorage.getItem('mofa-language');
      const defaultLang = 'en'; // 默认英文
      const targetLang = savedLang || defaultLang;
      const targetUrl = '/blog/' + targetLang + '/';
      window.location.replace(targetUrl);
    })();
  </script>
</head>
<body>
  <div style="text-align: center; padding: 80px 20px; font-family: system-ui, -apple-system, sans-serif;">
    <h1 style="font-size: 2rem; margin-bottom: 16px;">Redirecting to Blog...</h1>
    <p style="color: #666; margin-bottom: 24px;">
      If you are not redirected automatically,
      <a href="/blog/en/" style="color: #0066cc;">click here (English)</a>
      <span> / </span>
      <a href="/blog/zh/" style="color: #0066cc;">点击这里（中文）</a>
    </p>
    <div class="loading" style="margin: 0 auto; width: 40px; height: 40px; border: 4px solid #f3f3f3; border-top: 4px solid #0066cc; border-radius: 50%; animation: spin 1s linear infinite;"></div>
  </div>

  <style>
    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }
  </style>
</body>
</html>
