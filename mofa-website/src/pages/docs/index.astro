---
// 客户端根据语言自动重定向
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Redirecting to Documentation...</title>
  <script>
    // 根据 localStorage 语言状态重定向
    (function() {
      const isEnglish = localStorage.getItem('mofa-language') !== 'zh';
      const targetUrl = isEnglish ? '/docs/en/0.overview' : '/docs/zh/0.overview';
      window.location.replace(targetUrl);
    })();
  </script>
</head>
<body>
  <div style="text-align: center; padding: 80px 20px; font-family: system-ui, -apple-system, sans-serif;">
    <h1 style="font-size: 2rem; margin-bottom: 16px;" data-en="Redirecting to Documentation..." data-zh="正在跳转到文档...">Redirecting to Documentation...</h1>
    <p style="color: #666; margin-bottom: 24px;">
      <span data-en="If you are not redirected automatically, " data-zh="如果没有自动跳转，">If you are not redirected automatically, </span>
      <a href="/docs/en/0.overview" style="color: #0066cc;" data-en="click here (English)" data-zh="点击这里（英文）">click here (English)</a>
      <span> / </span>
      <a href="/docs/zh/0.overview" style="color: #0066cc;" data-en="点击这里（中文）" data-zh="点击这里（中文）">点击这里（中文）</a>
    </p>
    <div class="loading" style="margin: 0 auto; width: 40px; height: 40px; border: 4px solid #f3f3f3; border-top: 4px solid #0066cc; border-radius: 50%; animation: spin 1s linear infinite;"></div>
  </div>

  <style>
    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }
  </style>

  <script>
    // 根据语言更新页面文本
    document.addEventListener('DOMContentLoaded', function() {
      const isEnglish = localStorage.getItem('mofa-language') !== 'zh';
      document.querySelectorAll('[data-en]').forEach(el => {
        el.textContent = isEnglish ? el.getAttribute('data-en') : el.getAttribute('data-zh');
      });
    });
  </script>
</body>
</html> 